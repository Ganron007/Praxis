# Dependencies
node_modules/

# Dogfood artifacts (praxis scan . self-report)
praxis-report.html

# Archive (old reports, samples, generated artifacts)
archive/

# Claude Code local config (AGENTS.md is tracked — the cross-agent standard)
CLAUDE.md
.claude/

# Local-only working docs (not shipped in the public repo)
AGENTS.md
CHANGELOG.md

# Runtime state (scan context, undo/fix history, caches)
.praxis/
*.fixes.jsonl
~/.praxis/

# Test fixtures
fixtures/
showcase-target/
showcase-evidence/

# Local lab journal
SHOWCASE-LAB-LOG.md

# Internal planning/assessment docs (not for public release)
docs/internal/

# npm pack artifacts
*.tgz

# Secrets — never commit real API keys.
# .env holds your live keys (ignored); .env.example is the tracked template.
.env
.env.local
.env.*.local
!.env.example
